language: generic
sudo: required
services:
  - docker # Generally needed

# TAG can be anything (used inside docker.. MYTAG also works)
before_install:
  - docker build -t dbeken85/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dbeken85/docker-react npm run test
